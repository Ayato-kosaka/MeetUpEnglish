<h1>Editing Event</h1>

<%= render 'form', home: @home %>

<%= link_to 'Back', homes_schedule_path(selected_prefecture: City.find(@home.cityId).prefectureId), data: {"turbolinks"=>false}  %> |
<%= link_to 'Destroy', home_path(@home.id), method: :delete, data: { confirm: 'Are you sure?' } %>
