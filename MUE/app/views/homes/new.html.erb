<h1>Create new Meet Up English</h1>

<%= render 'form', home: @home %>


<%= link_to 'Back', homes_schedule_path(selected_prefecture: City.find(params[:selected_city]).prefectureId), data: {"turbolinks"=>false}  %>
