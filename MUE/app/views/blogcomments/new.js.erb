$("#create_comment").html("<%= escape_javascript(render 'new', { blogId: params[:blogId],blogcomment: @blogcomment}) %>")

$(function(){
 $(".modal-close-btn").click(function () {
   console.log( "click" );
   $("#overlay").hide();
 })
})
